[pytest]
addopts =
    -ra
    -q
    ; --html=.reports/unitests.html
    ; --self-contained-html
    ; --cov=app tests_unit/
    ; --cov-report html:.reports/coverage
filterwarnings =
    error
testpaths =
    tests